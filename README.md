# 📌 URL Shortener Microservices

This is a practice project to learn Go by building a simple URL shortener with two microservices. The Shortener Service accepts long URLs and generates short URLs, while the Redirector Service redirects users to the original URL when they visit a short link.